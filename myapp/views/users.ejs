<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
	<% for(user of users) { %>
	   <p><%= user.id %> - <%= user.name %></p>
	<% } %>
  </body>
</html>
